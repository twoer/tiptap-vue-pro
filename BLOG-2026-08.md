# tiptap-vue-pro 8 月回顾：三个版本，从代码块到自动保存

> 8 月是 tiptap-vue-pro 迭代最快的一个月：17 个提交、350 个文件变更、新增约 1.86 万行代码，连续发布了 0.1.8、0.1.9、0.2.0 三个版本。核心能力从「能用」走向「好用」，架构上把共享逻辑持续下沉到 core，三套 UI 适配层则越来越薄。

## 本月概览

| 指标 | 数值 |
| --- | --- |
| 提交数 | 17 |
| 变更文件 | 350 |
| 新增 / 删除行数 | +18,623 / -1,906 |
| 版本发布 | 0.1.8 → 0.1.9 → 0.2.0 |
| 主要功能 | 代码块工具栏、Mermaid 图表、自动保存与草稿恢复、紧凑工具栏 |

三条主线贯穿整个月：**代码块体验升级**（0.1.8）、**Mermaid 图表支持**（0.1.9）、**自动保存与本地草稿恢复**（0.2.0）。与此同时，架构上持续做「减法」——把三套适配层里重复的工具栏逻辑抽成 headless 控制器，并补上了 CI 质量门禁与 npm 包兼容性验证。

## 功能更新

### 代码块体验升级（0.1.8）

代码块从「能粘贴代码」升级为「方便读写代码」：

- **代码块上下文工具栏**：三套适配层（Element Plus、Naive UI、Ant Design Vue）各自实现原生气泡菜单，支持切换语言和一键复制代码。
- **语法高亮菜单扩展至 17 种语言**：Plain Text、JavaScript、TypeScript、HTML / Vue、CSS、JSON、Python、Java、C、C++、C#、Go、Rust、Bash、SQL、YAML、Markdown。
- **语言图标**：工具栏与语言菜单新增易识别的语言图标；自定义语言没有内置图标时，自动回退到通用代码图标。

### Mermaid 图表支持（0.1.9）

本月最大的新功能，为编辑器补齐了图表能力：

- 三套适配层新增独立 Mermaid 块，支持**代码、图表、分屏**三种视图，编辑与预览自由切换。
- 渲染层做了完整的安全与健壮性设计：语法校验、过期结果保护、严格安全模式、按需加载（体积不拖累首屏）、移动端响应式布局。
- 对应逻辑沉淀在 core：`mermaidCodeEditor`（代码编辑）、`mermaidRenderer`（渲染）、`mermaidNodeView`（节点视图）各司其职。

### 自动保存与本地草稿恢复（0.2.0）

面向真实使用场景的一次重要补强——**内容不会再因为误关页面而丢失**：

- **Autosave**：可选启用，支持防抖、串行请求（避免保存乱序）、latest-content-wins 冲突策略、显式重试、状态事件，以及在页面卸载时的尽力而为刷新（best-effort flush）。
- **版本化本地草稿**：自动保存的历史草稿按版本存放，支持过期校验、SSR 安全的浏览器存储、显式恢复与删除，并对异步过期结果做保护——防止较旧的远端保存成功后误删较新的本地草稿。
- 三套适配层同步补齐对等的自动保存与草稿恢复 UI，覆盖响应式与暗色模式。
- 新增中英文《自动保存》与《本地草稿》指南，并配套浏览器端到端测试（Playwright）。

### 紧凑工具栏布局

工具栏新增紧凑布局模式，由 core 的 `resolveToolbarCompactActions` 统一解析：图片按钮按是否支持上传/链接自动拆分为对应动作，分割线与 Markdown 动作按配置展开。三套适配层同步实现，小屏与密集场景下工具栏不再臃肿。

## 架构与质量优化

### 共享逻辑持续下沉 core

延续「headless composable + 适配层只做 UI 壳」的范式，本月又完成两轮抽离：

- **图片 URL 控制器共享**：三套工具栏里重复的图片 URL 状态管理统一抽到 core 的 `toolbarImageUrlController`，适配层只保留原生 UI 组件。
- **一批新 headless 控制器落位 core**：图片裁剪、资源输入、链接编辑、Markdown / 打印动作、查找替换状态等，均由 core 提供逻辑。仅工具栏脚本一项，就减少了 895 行三套适配层之间的重复代码。

### CI 质量门禁

持续集成从「只管文档部署」升级为完整质量门禁：**包构建、类型检查、单元测试、文档部署**全部纳入 CI；新版流程还加入了「打包产物验证」步骤，确保发布到 npm 的包在真实安装场景下可正常使用。

### npm 包兼容性加固

针对多打包器、多模块格式场景做了系统性加固：

- 新增 `package-exports-smoke` 冒烟脚本（约 500 行），模拟真实安装并验证每个包的导出声明与实际产物一致，已接入 CI。
- core 的产物改为 `rollupTypes: true`（生成完整类型声明），并设置 `interop: 'auto'` 兼容不同模块消费方式。

### 修复

- 修复编辑器内容区高度表现与气泡占位。
- 修复滚动时媒体气泡菜单未跟随选中节点的问题。

## 文档与站点

- 新增 Mermaid 使用指南，以及自动保存、本地草稿的中英文文档。
- 文档站接入百度统计与 51la，为后续内容运营做准备。

## 展望

0.2.0 之后，编辑器在功能层面已经相当完整。接下来值得投入的方向：一是继续把 `useToolbar` 等更大的共享逻辑抽成 headless 组合式函数（当前工具栏脚本仍是三套适配层中最大的一块）；二是让 E2E 测试进入常态化；三是收窄 core 的公共 API，把内部实现和对外契约分清楚。

---

*本文由 8 月 git 提交记录整理而成。项目地址：tiptap-vue-pro（Tiptap v3 + Vue 3 + TypeScript 的 monorepo 富文本编辑器组件库）。*
